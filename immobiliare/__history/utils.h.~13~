//---------------------------------------------------------------------------
#ifndef utilsH
#define utilsH

#include <vcl.h>

namespace utils
{
    AnsiString __fastcall GetDataPath();

	//bool __fastcall IsValidDate(AnsiString strDate);

	bool __fastcall ParseDate(AnsiString strDate,
    	unsigned short& nYear, unsigned short& nMonth, unsigned short& nDay);
};

//---------------------------------------------------------------------------
#endif
